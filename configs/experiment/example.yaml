# @package _global_
#
# Example experiment config (Rule 41).
# Run: python src/my_project/train.py experiment=example
#
# This overrides any defaults set in train.yaml, data/default.yaml, etc.

seed: 42

data:
  batch_size: 128
  num_workers: 4

model:
  hidden_dim: 256
  lr: 1e-3
  weight_decay: 1e-4

trainer:
  max_epochs: 50
