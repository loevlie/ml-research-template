# Your torch model behind the sklearn interface (see sklearn_wrapper.py).
_target_: {{ package_name }}.sklearn_wrapper.TorchMLPClassifier
hidden_dim: 128
lr: 1e-3
max_epochs: 50
random_state: ${seed}
