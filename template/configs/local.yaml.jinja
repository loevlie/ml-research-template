# Machine-local overrides — gitignored, applied between the experiment preset
# and CLI args (utils/cli.py). Keys mirror the config schema (configs.py);
# ${a.b} strings resolve against the composed config.
#
# data:
#   num_workers: 0        # laptop
# trainer:
#   devices: 1
