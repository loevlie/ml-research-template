{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Home","text":"uv tool install copiercopier copy --trust gh:loevlie/ml-research-template my-project\ud83c\udfa4 project_name? Wine Quality\ud83c\udfa4 framework? pytorch\ud83c\udfa4 logger? wandb\"Wine Quality\" is ready at my-projectcd my-projectuv run python src/wine_quality/train.pyEpoch   0 | train_loss=2.3055 | val_loss=2.2940 | val_acc=0.0850 <p>Ten prompts, thirty seconds: git initialized, dependencies locked, pre-commit installed, training.</p>"},{"location":"#start-every-project-ready-to-publish","title":"Start every project ready to publish","text":"<p>A Copier template for ML research \u2014 PyTorch or JAX, typed configs, uv \u2014 with multi-seed significance testing and <code>copier update</code> baked in.</p> <p>Get started 15-minute tutorial Why this stack</p>"},{"location":"#what-you-get","title":"What you get","text":"<ul> <li> <p> An explicit training loop</p> <p>~60 readable lines on Lightning Fabric \u2014 device placement, mixed precision, and DDP without a <code>Trainer</code> black box. Resume, LR scheduling, gradient accumulation, and clipping are wired in and config-driven. Prefer JAX? <code>framework=jax</code> swaps the core, keeps everything else.</p> <p> Train &amp; experiment</p> </li> <li> <p> Statistical rigor on day one</p> <p>Multi-seed launchers (local + SLURM array) and an aggregator with bootstrap CIs, paired Wilcoxon/t-tests, and Cohen's d. Publication-ready numbers, not single-seed anecdotes.</p> <p> Multi-seed statistics</p> </li> <li> <p> Typed configs, Hydra-style CLI</p> <p>pydantic schemas your IDE checks, with the familiar <code>experiment=x loss=contrastive model.lr=1e-3</code> syntax kept \u2014 typos die at parse time with a suggestion, not at epoch 40.</p> <p> Typed configs &amp; overrides</p> </li> <li> <p> SLURM-native, preemption-proof</p> <p>Sweeps and Optuna searches fan out as job arrays via submitit; sbatch scripts cover the rest. Preempted jobs requeue into the same run directory and continue from <code>last.ckpt</code>.</p> <p> Run on SLURM</p> </li> <li> <p> Domain flavors</p> <p><code>tabular</code>: OpenML tasks, an sklearn-estimator wrapper, TabPFN/TabICL baselines, and a code-version-aware cached benchmark harness. <code>multimodal</code>: timm / open_clip / HF datasets and a CLIP-style objective.</p> <p> Tabular benchmarking</p> </li> <li> <p> <code>copier update</code></p> <p>Pull template improvements into existing projects without clobbering your code \u2014 the compounding win across a PhD's worth of projects. Runtime shape checking (jaxtyping + beartype) rides along everywhere.</p> <p> Updating projects</p> </li> </ul>"},{"location":"#is-it-for-you","title":"Is it for you?","text":"<p>Use it if you\u2026</p> <ul> <li> start more than one project \u2014 <code>copier update</code> compounds</li> <li> want every paper number backed by seeds + significance tests</li> <li> edit training loops weekly and want to read yours</li> <li> run on a SLURM cluster (or will soon)</li> </ul> <p>Skip it if you\u2026</p> <ul> <li> want a callback-driven <code>Trainer</code> \u2014 use lightning-hydra-template</li> <li> work in TensorFlow</li> <li> don't want to own the CLI code (one ~250-line file you can read)</li> <li> need multi-node pretraining scaffolding \u2014 crib torchtitan</li> </ul>"},{"location":"cheatsheet/","title":"Cheatsheet","text":"<p>Every common command on one page. <code>&lt;pkg&gt;</code> is your package name; drop the <code>uv run</code> prefix if your venv is activated. Everything here works identically on PyTorch and JAX projects (<code>trainer.precision</code>/<code>devices</code> are PyTorch-specific \u2014 JAX handles both automatically).</p>"},{"location":"cheatsheet/#train","title":"Train","text":"<pre><code>uv run python src/&lt;pkg&gt;/train.py                            # base config\nuv run python src/&lt;pkg&gt;/train.py experiment=example         # named preset\nuv run python src/&lt;pkg&gt;/train.py model.lr=1e-3 seed=123     # override anything\nuv run python src/&lt;pkg&gt;/train.py loss=contrastive           # swap a config group\nuv run python src/&lt;pkg&gt;/train.py trainer.precision=bf16-mixed\nuv run python src/&lt;pkg&gt;/train.py run_dir=outputs/myrun trainer.resume=auto\nuv run python src/&lt;pkg&gt;/train.py --help                     # all flags + presets + variants\n</code></pre>"},{"location":"cheatsheet/#evaluate","title":"Evaluate","text":"<pre><code>uv run python src/&lt;pkg&gt;/eval.py ckpt_path=outputs/&lt;run&gt;/best.ckpt\n# training config auto-restored from the run's config.yaml; overrides still win\n</code></pre>"},{"location":"cheatsheet/#seeds-statistics","title":"Seeds &amp; statistics","text":"<pre><code>bash scripts/run_seeds.sh experiment=ours seeds=\"42,123,456,789,1337\"\nsbatch scripts/sbatch_seeds.sh experiment=ours              # same, as a SLURM array\nuv run python scripts/aggregate_seeds.py outputs/multi_seed_&lt;stamp&gt; \\\n    --baseline outputs/multi_seed_baseline --metric val/acc\n</code></pre>"},{"location":"cheatsheet/#sweeps-tuning","title":"Sweeps &amp; tuning","text":"<pre><code>uv run python scripts/sweep.py seed=42,123 model.lr=1e-4,1e-3      # cross product, local\nuv run python scripts/sweep.py --cluster slurm --partition gpu \\\n    seed=42,123 model.lr=1e-4,1e-3                                  # same, one SLURM array\nuv run python scripts/tune.py --n-trials 20                         # Optuna, local\nuv run python scripts/tune.py --n-trials 64 --workers 8 \\\n    --cluster slurm --partition gpu                                 # 8 parallel workers\n</code></pre>"},{"location":"cheatsheet/#slurm","title":"SLURM","text":"<pre><code>uv sync --extra cluster                                # one-time: submitit\nsbatch scripts/sbatch_train.sh experiment=example      # single job\nsbatch scripts/sbatch_seeds.sh experiment=example      # seed array\n# preempted jobs requeue + resume automatically (run_dir pinned, resume=auto)\n</code></pre>"},{"location":"cheatsheet/#tracking","title":"Tracking","text":"<pre><code>uv run python src/&lt;pkg&gt;/train.py logger.kind=wandb       # cloud (uv sync --extra tracking-wandb)\nuv run python src/&lt;pkg&gt;/train.py logger.kind=trackio     # local-first; dashboard: uv run trackio show\nuv run python src/&lt;pkg&gt;/train.py logger.kind=csv         # zero deps\nWANDB_MODE=offline sbatch scripts/sbatch_train.sh ...    # air-gapped nodes; wandb sync later\n</code></pre>"},{"location":"cheatsheet/#tabular-flavor","title":"Tabular flavor","text":"<pre><code>uv run python src/&lt;pkg&gt;/benchmark.py estimator=logreg task_id=31 fold=0   # one cached cell\nbash scripts/run_benchmark.sh                                             # local grid\nsbatch scripts/sbatch_benchmark.sh                                        # grid as SLURM array\nuv run python scripts/aggregate_benchmark.py outputs/benchmark_&lt;id&gt; --baseline logreg\nuv sync --extra tabular-baselines                                         # TabPFN + TabICL\n</code></pre>"},{"location":"cheatsheet/#project-hygiene","title":"Project hygiene","text":"<pre><code>uv run pytest                       # smoke tests (overfit-one-batch, init loss, shapes)\nuv run ruff check . &amp;&amp; uv run ruff format .\nuv run mypy src\ncopier update --trust               # pull template improvements into this project\n</code></pre>"},{"location":"cheatsheet/#where-things-land","title":"Where things land","text":"<pre><code>outputs/&lt;date&gt;/&lt;time&gt;/              # or your run_dir=...\n\u251c\u2500\u2500 config.yaml                     # resolved config + git sha + argv (the provenance snapshot)\n\u251c\u2500\u2500 best.ckpt / last.ckpt           # model + optimizer + scheduler + counters\n\u251c\u2500\u2500 metrics.json                    # final metrics (read by aggregate_seeds.py)\n\u2514\u2500\u2500 csv_logs/ | wandb/ | ...        # logger output\n</code></pre>"},{"location":"getting-started/","title":"Getting started","text":""},{"location":"getting-started/#prerequisites","title":"Prerequisites","text":"<p>uv is the only hard requirement \u2014 it installs Python itself if needed:</p> <pre><code>curl -LsSf https://astral.sh/uv/install.sh | sh\nuv tool install copier\n</code></pre>"},{"location":"getting-started/#generate-a-project","title":"Generate a project","text":"<pre><code>copier copy --trust gh:loevlie/ml-research-template my-project\n</code></pre> <p><code>--trust</code></p> <p>The template runs four post-generation tasks: <code>git init</code>, <code>uv lock</code>, <code>uv sync --extra dev</code>, and <code>pre-commit install</code>. <code>--trust</code> allows that. Drop the flag to run them yourself.</p>"},{"location":"getting-started/#the-prompts","title":"The prompts","text":"Prompt Default What it controls <code>project_name</code> \u2014 Human-readable name (\"Retinal OCT Classifier\") <code>package_name</code> derived Import name (<code>retinal_oct_classifier</code>) <code>project_description</code> generic <code>pyproject.toml</code> + README <code>author_name</code> / <code>author_email</code> \u2014 LICENSE + pyproject authors <code>python_version</code> <code>3.12</code> Pins <code>.python-version</code>, <code>requires-python</code>, ruff/mypy targets (3.11\u20133.13) <code>framework</code> <code>pytorch</code> Deep learning library: <code>pytorch</code> or <code>jax</code> \u2014 see JAX <code>cuda_version</code> <code>cu124</code> GPU wheel routing (<code>cpu</code>, <code>cu118</code>\u2013<code>cu128</code>); for jax, any CUDA choice installs <code>jax[cuda12]</code> <code>logger</code> <code>wandb</code> Default tracker: <code>wandb</code>, <code>trackio</code>, <code>tensorboard</code>, <code>csv</code> \u2014 see Tracking <code>flavor</code> <code>generic</code> Domain scaffolding: <code>generic</code>, <code>tabular</code>, <code>multimodal</code> (PyTorch only \u2014 skipped for jax) <code>include_example</code> <code>true</code> MLP reference example, smoke tests, demo, per-project docs <code>include_dennys_rules</code> <code>false</code> A research operating manual as <code>DENNYS_RULES.md</code>"},{"location":"getting-started/#pick-a-flavor","title":"Pick a flavor","text":"generictabularmultimodal <p>The base template: training loop, configs, multi-seed stats, SLURM scripts. Right for most projects.</p> <p>Adds OpenML task loading with standardized CV folds, an sklearn-estimator wrapper for your torch models, foundation-model baselines (TabPFN, TabICL) as an extra, and an estimator \u00d7 task \u00d7 fold benchmark harness with SLURM-array support. See Tabular benchmarking.</p> <p>Adds timm / open_clip / transformers / HF datasets as dependencies, an image-text dataloader with CLIP-matched preprocessing, and pairs with the CLIP-style contrastive objective. See Multimodal.</p> <p>The flavors build on PyTorch libraries. Choosing <code>framework=jax</code> gives you the generic project with a flax NNX + optax training core instead \u2014 same configs, same commands (what changes).</p>"},{"location":"getting-started/#first-training-run","title":"First training run","text":"cd my-projectuv run python src/&lt;package_name&gt;/train.pyrun dir   outputs/2026-06-12/10-53-13model     ExampleModel | 22,026 params | lr=0.0003Epoch   0 | train_loss=2.3055 | val_loss=2.2940 | val_acc=0.0850best      val_loss=2.2925 | ckpts outputs/.../best.ckpt <p>That trains the reference MLP on synthetic data \u2014 proof the whole pipeline works before you touch anything. Then:</p> <pre><code>uv run python src/&lt;package_name&gt;/train.py model.lr=1e-3 data.batch_size=128   # CLI overrides\nuv run python src/&lt;package_name&gt;/train.py experiment=example                  # named experiment\nuv run pytest                                                                 # smoke tests\n</code></pre> <p>Every run lands in <code>outputs/&lt;date&gt;/&lt;time&gt;/</code> with the resolved config, logs, <code>best.ckpt</code>, <code>last.ckpt</code>, and <code>metrics.json</code>.</p>"},{"location":"getting-started/#make-it-yours","title":"Make it yours","text":"<p>Two factory functions are the seams between your code and the entry points \u2014 replace their bodies and you never touch <code>train.py</code> or <code>eval.py</code>:</p> <ol> <li>Data \u2014 <code>create_dataloaders(cfg, seed)</code> in <code>src/&lt;pkg&gt;/data/datamodule.py</code>. Swap in your dataset; new knobs (paths, transforms) become fields on <code>DataConfig</code> in <code>configs.py</code>.</li> <li>Model \u2014 <code>build_model(cfg)</code> in <code>src/&lt;pkg&gt;/models/module.py</code>. Swap in your architecture (keep the <code>@jaxtyped</code> shape annotations); new knobs go on <code>ModelConfig</code>.</li> <li>Loss \u2014 if it isn't plain cross-entropy, add an objective \u2014 see Train &amp; experiment.</li> </ol> <p>All of these files are listed in the template's <code>_skip_if_exists</code>, so future <code>copier update</code> runs never overwrite them.</p> <p>See it done end-to-end</p> <p>The tutorial walks this exact path with a real dataset \u2014 from <code>copier copy</code> to a multi-seed confidence interval in ~15 minutes.</p>"},{"location":"stack/","title":"The stack (and why)","text":"<p>Every tool re-evaluated against the 2026 landscape. One picture first: the parts you own sit behind two factory seams, everything else is shared plumbing the template maintains for you.</p> <pre><code>flowchart TB\n    subgraph own [\"you own (never overwritten by copier update)\"]\n        direction LR\n        DM[\"datamodule.py\"]\n        MM[\"models/module.py\"]\n        OB[\"objectives.py\"]\n        CF[\"configs.py + experiments.py\"]\n    end\n    subgraph tpl [\"template-owned (improves with copier update)\"]\n        direction LR\n        TR[\"train.py / eval.py\"]\n        LP[\"training_loop.py\"]\n        CLI[\"utils/cli.py&lt;br&gt;&lt;i&gt;Hydra-style CLI on tyro&lt;/i&gt;\"]\n        OR[\"sweep.py \u00b7 tune.py \u00b7 run_seeds&lt;br&gt;&lt;i&gt;submitit \u00b7 optuna \u00b7 stats&lt;/i&gt;\"]\n    end\n    CF --&gt; CLI --&gt; TR\n    DM --&gt; TR\n    MM --&gt; TR\n    OB --&gt; LP\n    TR --&gt; LP\n    TR --&gt; OR</code></pre> Concern Choice One-line why Packaging uv Won the ecosystem; lockfiles + CUDA wheel routing built in Config pydantic + tyro (utils/cli.py) Typed, IDE-checked configs with Hydra's CLI ergonomics kept Sweeps / SLURM submitit + optuna (owned glue scripts) No frozen plugins; every line readable Training Lightning Fabric (own loop) Distributed/AMP plumbing without a Trainer black box Framework PyTorch default, JAX optional Ecosystem by default; XLA + explicit keys when a project wants them Caching exca + codever (tabular) Benchmark cells memoized, keyed to config and code version Tracking wandb default, trackio local fallback Best academic UX; a credible local escape hatch Shapes jaxtyping + beartype Runtime shape checks; bugs die at the first forward pass Lint/format ruff One fast tool replaces black + isort + flake8 Stats scipy (+ pingouin extra) Paired Wilcoxon, bootstrap CIs, Cohen's d out of the box"},{"location":"stack/#typed-configs-and-why-we-left-hydra","title":"Typed configs \u2014 and why we left Hydra","text":"<p>This template ran on Hydra until mid-2026. Hydra's last feature release was early 2023 and its plugin ecosystem froze, while the field moved to typed configs (torchtitan, LeRobot, nerfstudio). We migrated to pydantic + tyro and kept the two things Hydra did best:</p> <ul> <li>the free-order <code>key=value</code> CLI with group swaps \u2014 <code>experiment=example loss=contrastive model.lr=1e-3</code> still works verbatim, now typo-checked at parse time</li> <li><code>${...}</code> interpolation \u2014 replaced by one visible <code>resolved()</code> method in code, still supported inside <code>configs/local.yaml</code></li> </ul> <p>What Hydra's launcher/sweeper plugins did is now a few hundred lines of owned glue (<code>sweep.py</code>, <code>tune.py</code>, <code>run_dir.py</code>) on actively maintained deps.</p>"},{"location":"stack/#lightning-fabric-not-trainer","title":"Lightning Fabric, not Trainer","text":"<p>Research code in 2026 is hand-written loops with a thin distributed layer underneath \u2014 callback frameworks fight you on custom objectives and <code>torch.compile</code>. Fabric does device placement, mixed precision, DDP, and checkpoint I/O; the loop stays ~75 readable lines, with resume, LR scheduling, accumulation, and clipping config-driven. The <code>Objective</code> protocol keeps the loop loss-agnostic: any callable <code>(model, batch) -&gt; {\"loss\": ...}</code> plugs in via <code>loss=&lt;name&gt;</code>.</p>"},{"location":"stack/#pytorch-by-default-jax-by-choice","title":"PyTorch by default, JAX by choice","text":"<p>The ecosystems this template serves are PyTorch-shaped \u2014 TabPFN/TabICL, timm, open_clip, and the sklearn-adoption path all assume torch artifacts. But nothing in the orchestration layer imports a framework, so <code>framework=jax</code> swaps just the training core: flax NNX model, optax optimizer (schedule/clipping/accumulation become optimizer composition), jitted steps. Choose it for small-model/many-step work, TPUs, or <code>vmap</code>-shaped research \u2014 details in JAX.</p>"},{"location":"stack/#exca-codever-tabular-flavor","title":"exca + codever (tabular flavor)","text":"<p>Benchmark cells are memoized on disk by exca, keyed on the estimator config, task, fold, seed \u2014 and the code version. <code>utils/codever.py</code> fingerprints your package's AST at submit time, auto-bumps <code>0.0.N-&lt;hash&gt;</code> on semantic edits, and appends a diff changelog: any cached number traces to the exact code that produced it, and reverting code resurrects the old cache. Details in Tabular benchmarking.</p>"},{"location":"stack/#tracking-wandb-trackio","title":"Tracking: wandb + trackio","text":"<p>The landscape moved: Neptune shut down (OpenAI acquisition, March 2026); W&amp;B went to CoreWeave but its free academic tier remains the best UX; HuggingFace's trackio is a local-first, wandb-compatible tracker. The template keeps trackers swappable (<code>logger.kind=...</code>) so vendor risk stays one config key deep \u2014 see Experiment tracking.</p>"},{"location":"stack/#jaxtyping-beartype","title":"jaxtyping + beartype","text":"<p><code>Float[Tensor, \"batch features\"]</code> annotations are checked at runtime on every call \u2014 the standard cure for silent broadcasting bugs (and framework-agnostic, despite the name). Dev extras add <code>lovely-tensors</code>, <code>torchinfo</code>, and <code>hypothesis-torch</code> for property-based tests.</p>"},{"location":"stack/#what-we-deliberately-skip","title":"What we deliberately skip","text":"<ul> <li>Lightning Trainer / Composer / HF Trainer \u2014 wrong altitude for novel-method research</li> <li>DVC \u2014 OpenML task IDs are the data versioning for tabular work; HF Hub for big blobs</li> <li>Multi-node DDP scaffolding \u2014 crib torchtitan when you need it</li> <li>SkyPilot / snakemake \u2014 watch list; adopt per-project if a need appears</li> </ul>"},{"location":"tour/","title":"Hands-on tour","text":"<p>The tutorial ended with a number. This tour exercises everything around it \u2014 sweeps, Optuna, crash recovery, tracker swapping, and a paired significance test \u2014 in the same wine-quality project, ~20 minutes, all on a laptop. Every output below is from a real run.</p>"},{"location":"tour/#1-sweep-a-grid","title":"1 \u00b7 Sweep a grid","text":"<p>Comma-separated values fan out as a cross product, each point in its own run directory:</p> <pre><code>uv run python scripts/sweep.py experiment=wine model.lr=1e-4,1e-3 seed=42,123 trainer.max_epochs=10\ngrep -r \"val/acc\" outputs/sweep_*/run_*/metrics.json\n</code></pre> <pre><code>run_000_34788291:  \"val/acc\": 0.583    # lr=1e-4 seed=42\nrun_001_14355ae5:  \"val/acc\": 0.580    # lr=1e-4 seed=123\nrun_002_578de15c:  \"val/acc\": 0.624    # lr=1e-3 seed=42\nrun_003_76207663:  \"val/acc\": 0.621    # lr=1e-3 seed=123\n</code></pre> <p>Two seeds per point already tell you something a single run can't: the lr=1e-3 advantage (~0.04) is bigger than the seed wobble (~0.006) \u2014 worth pursuing. Add <code>--cluster slurm</code> and this exact command becomes a job array.</p>"},{"location":"tour/#2-let-optuna-search","title":"2 \u00b7 Let Optuna search","text":"<p>The search space lives in <code>suggest()</code> inside <code>scripts/tune.py</code> \u2014 edit it like code, then:</p> <pre><code>uv run python scripts/tune.py --n-trials 5\n</code></pre> <pre><code>tune: best value 0.6552 with {'model.lr': 9.31e-03, 'model.hidden_dim': 512,\n'model.weight_decay': 1.26e-05, 'data.batch_size': 64}\n</code></pre> <p>Five trials and it beat both of our hand-picked configs \u2014 with a wider model than we tried. Workers share the study through a journal file, so <code>--workers 8 --cluster slurm</code> parallelizes it with no database server.</p>"},{"location":"tour/#3-kill-it-resume-it","title":"3 \u00b7 Kill it, resume it","text":"<p>Pin a run directory and ask for auto-resume, then kill the run mid-training (<code>Ctrl-C</code>, or a SLURM preemption \u2014 same thing):</p> <pre><code>uv run python src/wine_quality/train.py experiment=wine \\\n    run_dir=outputs/long trainer.resume=auto trainer.max_epochs=300\n# ... training ... ^C\n</code></pre> <p>Run the same command again:</p> <pre><code>Resumed from outputs/long/last.ckpt (epoch 51)\nEpoch  52 | train_loss=0.5834 | val_loss=0.9894 | val_acc=0.6395\nEpoch  53 | train_loss=0.5810 | val_loss=0.9742 | val_acc=0.6364\n</code></pre> <p>The loss continues falling smoothly because <code>last.ckpt</code> carries the optimizer moments and scheduler position, not just weights. On SLURM the sbatch scripts wire this up for you \u2014 preempted jobs heal themselves (how).</p>"},{"location":"tour/#4-swap-the-tracker","title":"4 \u00b7 Swap the tracker","text":"<pre><code>uv sync --extra tracking-trackio\nuv run python src/wine_quality/train.py experiment=wine logger.kind=trackio\n</code></pre> <pre><code>* Trackio project initialized: wine_quality\n* Trackio metrics logged to: ~/.cache/huggingface/trackio\n</code></pre> <p><code>uv run trackio show</code> opens a local dashboard \u2014 no account, no cloud. The training code didn't change; trackers are one config key (tracking).</p>"},{"location":"tour/#5-the-honest-finale","title":"5 \u00b7 The honest finale","text":"<p>Is the <code>wine</code> preset (lr=1e-3) actually better than the default config at the same budget? Run both on the same seeds and let the statistics answer:</p> <pre><code>bash scripts/run_seeds.sh experiment=wine seeds=\"42,123,456,789,1337\"\nbash scripts/run_seeds.sh experiment=base trainer.max_epochs=30 seeds=\"42,123,456,789,1337\"\nuv run python scripts/aggregate_seeds.py outputs/multi_seed_&lt;wine&gt; \\\n    --baseline outputs/multi_seed_&lt;base&gt; --metric val/acc\n</code></pre> <pre><code>--- Paired Comparison (shared seeds: [42, 123, 456, 789, 1337]) ---\nOurs:     0.6219 +/- 0.0322\nBaseline: 0.6169 +/- 0.0165\nDelta:    +0.0050\nTest:     paired t-test (fallback, n&lt;6 for Wilcoxon)\nStat:     0.6610, p=0.5448\nEffect:   Cohen's d = 0.196\nNOT significant at p&lt;0.05\n</code></pre> <p>A +0.5% delta that doesn't survive the test \u2014 at this budget, our hand-tuned lr is seed noise, and the machinery just stopped us from claiming otherwise. (Note the t-test fallback firing at 5 seeds \u2014 that's the <code>2/2^N</code> floor from Statistics, explained.)</p> <p>Your move: promote Optuna's winner from step 2 into <code>experiments.py</code> and re-run this comparison with 8 seeds. That's not an exercise anymore \u2014 that's research, and now the whole pipeline is muscle memory.</p>"},{"location":"tutorial/","title":"Tutorial: real data in 15 minutes","text":"<p>Generate a project, swap in a real dataset, and finish with a publication-grade number \u2014 mean accuracy with a bootstrap confidence interval over 5 seeds. Every command and output below is from an actual run.</p> <p>We'll predict red-wine quality from 11 physicochemical measurements (UCI wine quality, 1,599 rows \u2014 small enough to train on a laptop CPU in seconds).</p> <p>Two other ways to take this tutorial</p> <p>Interactive walkthrough \u2014 a scrollable code tour where each step lights up the exact lines doing the work. The repo \u2014 the finished project, one commit per step; the step-2 diff is the whole \"bring your own data\" change at once.</p>"},{"location":"tutorial/#1-generate-and-smoke-test","title":"1 \u00b7 Generate and smoke-test","text":"<pre><code>uv tool install copier\ncopier copy --trust gh:loevlie/ml-research-template wine-quality\ncd wine-quality\nuv run python src/wine_quality/train.py trainer.max_epochs=2\n</code></pre> <p>That trains the reference MLP on synthetic data \u2014 proof the pipeline works before you touch anything.</p>"},{"location":"tutorial/#2-bring-your-own-data","title":"2 \u00b7 Bring your own data","text":"<p>You edit exactly two files, and both are yours \u2014 <code>copier update</code> never overwrites them. The entry points (<code>train.py</code>, <code>eval.py</code>) only ever call two factory functions, so they stay untouched:</p> <pre><code>flowchart LR\n    subgraph yours [\"your files (never overwritten)\"]\n        C[\"configs.py&lt;br&gt;DataConfig \u00b7 ModelConfig\"]\n        D[\"datamodule.py&lt;br&gt;create_dataloaders(cfg, seed)\"]\n        M[\"models/module.py&lt;br&gt;build_model(cfg)\"]\n    end\n    T[\"train.py / eval.py&lt;br&gt;&lt;i&gt;template-owned, updated for you&lt;/i&gt;\"] --&gt; D\n    T --&gt; M\n    C --&gt; D\n    C --&gt; M</code></pre> <p>First, describe the data in <code>DataConfig</code> (<code>src/wine_quality/configs.py</code>):</p> src/wine_quality/configs.py<pre><code>class DataConfig(pydantic.BaseModel):\n    csv_path: str = \"data/winequality-red.csv\"\n    n_features: int = 11  # the 11 physicochemical measurements\n    n_classes: int = 6  # quality scores 3-8\n    batch_size: int = 64\n    num_workers: int = 4\n    val_split: float = 0.2\n</code></pre> <p><code>n_features</code> and <code>n_classes</code> do real work: <code>TrainConfig.resolved()</code> copies them into the model config, so the MLP sizes its input and output layers to the data automatically. And because <code>csv_path</code> is a config field, it's now a CLI flag (<code>data.csv_path=...</code>) and lands in every run's provenance snapshot.</p> <p>Second, replace the placeholder dataset in <code>src/wine_quality/data/datamodule.py</code>:</p> src/wine_quality/data/datamodule.py<pre><code>WINE_URL = \"https://archive.ics.uci.edu/ml/machine-learning-databases/wine-quality/winequality-red.csv\"\n\n\nclass WineQualityDataset(Dataset):\n    \"\"\"UCI red wine quality: 11 physicochemical features -&gt; quality score 3-8.\"\"\"\n\n    def __init__(self, csv_path: str) -&gt; None:\n        path = Path(csv_path)\n        if not path.exists():  # download once, cache locally\n            path.parent.mkdir(parents=True, exist_ok=True)\n            urllib.request.urlretrieve(WINE_URL, path)\n        rows = np.loadtxt(path, delimiter=\";\", skiprows=1)\n        x = torch.tensor(rows[:, :-1], dtype=torch.float32)\n        self.data = (x - x.mean(0)) / x.std(0)  # standardize features\n        self.targets = torch.tensor(rows[:, -1], dtype=torch.long) - 3  # 3-8 -&gt; 0-5\n\n    def __len__(self) -&gt; int:\n        return len(self.data)\n\n    def __getitem__(self, idx: int) -&gt; tuple[torch.Tensor, torch.Tensor]:\n        return self.data[idx], self.targets[idx]\n</code></pre> <p>\u2026and point the factory at it (one line in <code>create_dataloaders</code>):</p> <pre><code>    dataset = WineQualityDataset(cfg.csv_path)\n</code></pre> <p>The model side needs nothing today \u2014 the example MLP already sizes itself from the config. When you bring your own architecture, you'll swap it inside <code>build_model()</code> in <code>models/module.py</code>, the same one-seam move.</p>"},{"location":"tutorial/#3-train","title":"3 \u00b7 Train","text":"<pre><code>uv run python src/wine_quality/train.py trainer.max_epochs=30 run_dir=outputs/wine_first\n</code></pre> <pre><code>------------------------------------------------------------------------\nrun dir   outputs/wine_first\nmodel     ExampleModel | 18,822 params | lr=0.0003\ntrainer   max 30 epochs | 32-true | seed=42\nloss      SupervisedLossConfig | logger csv\nconfig    outputs/wine_first/config.yaml (full snapshot + git state)\n------------------------------------------------------------------------\nEpoch   0 | train_loss=1.7181 | val_loss=1.5782 | val_acc=0.5486\nEpoch   1 | train_loss=1.4689 | val_loss=1.3399 | val_acc=0.5674\n...\nEpoch  29 | train_loss=0.8658 | val_loss=0.9371 | val_acc=0.6520\n------------------------------------------------------------------------\nbest      val_loss=0.9370 | val_acc=0.6426\nckpts     outputs/wine_first/best.ckpt (+ last.ckpt)\nevaluate  uv run python src/wine_quality/eval.py ckpt_path=outputs/wine_first/best.ckpt\n------------------------------------------------------------------------\n</code></pre> <p>Three lines in that output earn their place: <code>config</code> points at the run's provenance snapshot (resolved config + git SHA + the exact command \u2014 how you re-run this result months later), <code>ckpts</code> is where <code>best.ckpt</code> and <code>last.ckpt</code> land every epoch, and <code>evaluate</code> is ready to paste:</p> <pre><code>uv run python src/wine_quality/eval.py ckpt_path=outputs/wine_first/best.ckpt\n</code></pre> <pre><code>eval: settings restored from outputs/wine_first/config.yaml\nEval | loss=0.9370 | acc=0.6426\n</code></pre> <p>The first line is the snapshot doing its job: eval read the training run's config (including your <code>csv_path</code>) instead of making you re-specify it, and the numbers match training exactly.</p>"},{"location":"tutorial/#4-name-the-experiment","title":"4 \u00b7 Name the experiment","text":"<p>Promote the settings into a version-controlled preset in <code>src/wine_quality/experiments.py</code>, so the result traces to a commit:</p> src/wine_quality/experiments.py<pre><code>EXPERIMENTS: dict[str, TrainConfig] = {\n    \"base\": TrainConfig(),\n    \"wine\": TrainConfig(\n        model=ModelConfig(lr=1e-3),\n        trainer=TrainerConfig(max_epochs=30, patience=10),\n    ),\n}\n</code></pre> <pre><code>uv run python src/wine_quality/train.py experiment=wine\n</code></pre>"},{"location":"tutorial/#5-the-publication-grade-number","title":"5 \u00b7 The publication-grade number","text":"<p>One seed is an anecdote. Run five, get a confidence interval:</p> <pre><code>bash scripts/run_seeds.sh experiment=wine seeds=\"42,123,456,789,1337\"\nuv run python scripts/aggregate_seeds.py outputs/multi_seed_&lt;stamp&gt; --metric val/acc\n</code></pre> <pre><code>Metric: val/acc\nSeeds:  [123, 1337, 42, 456, 789]\nMean:   0.6219\nStd:    0.0322\n95% CI: [0.6044, 0.6520]\n</code></pre> <p>That's the line that goes in the paper: 0.622 \u00b1 0.032 (5 seeds) \u2014 and when you have a baseline to beat, run it with the same seeds and pass <code>--baseline</code> to get the paired significance test and effect size (how \u00b7 what the numbers mean).</p>"},{"location":"tutorial/#where-to-go-from-here","title":"Where to go from here","text":"<p>Keep this project \u2014 the hands-on tour continues it through sweeps, Optuna, crash recovery, tracker swapping, and a real paired significance test (~20 more minutes). Or jump to a topic:</p> <ul> <li> <p> Find better hyperparameters \u2014     grid sweeps and Optuna search, locally or as SLURM arrays.      Sweeps &amp; HP search</p> </li> <li> <p> Move to the cluster \u2014 the same commands,     preemption-proof.      Run on SLURM</p> </li> <li> <p> Watch runs live \u2014 switch trackers with     one flag (<code>logger.kind=wandb</code>).      Experiment tracking</p> </li> <li> <p> Custom objectives \u2014 contrastive,     masked, anything: swap the loss without touching the loop.      Train &amp; experiment</p> </li> </ul>"},{"location":"updating/","title":"Updating projects","text":"<p>The template improves; your projects can follow. This is the reason to use Copier over a \"Use this template\" button \u2014 and the compounding win when you run several projects off one template.</p> <pre><code>cd path/to/existing-project\ncopier update --trust\n</code></pre> <p>Copier reads <code>.copier-answers.yml</code> (your original answers + template version), computes a three-way merge between the old template, the new template, and your project, and applies it:</p> <pre><code>flowchart LR\n    A[\"template @ v1&lt;br&gt;&lt;i&gt;what you generated from&lt;/i&gt;\"] --&gt; M{\"three-way&lt;br&gt;merge\"}\n    B[\"template @ v2&lt;br&gt;&lt;i&gt;what improved since&lt;/i&gt;\"] --&gt; M\n    P[\"your project&lt;br&gt;&lt;i&gt;your edits&lt;/i&gt;\"] --&gt; M\n    M --&gt; U[\"updated project&lt;br&gt;&lt;i&gt;your files untouched,&lt;br&gt;conflicts marked&lt;/i&gt;\"]</code></pre>"},{"location":"updating/#whats-protected","title":"What's protected","text":"<p>Files you own are listed in the template's <code>_skip_if_exists</code> and are never overwritten on update \u2014 your model, datamodule, objectives, sklearn wrapper, experiment configs, README. Everything else (CI workflows, pre-commit config, training-loop improvements, new scripts) updates in place.</p>"},{"location":"updating/#handling-conflicts","title":"Handling conflicts","text":"<p>Where the template and your edits touch the same lines, Copier leaves standard conflict markers or <code>.rej</code> files:</p> <pre><code>grep -rn \"&lt;&lt;&lt;&lt;&lt;&lt;&lt;\" . --include=\"*.py\" --include=\"*.yaml\"   # find conflict markers\n</code></pre> <p>Resolve them like any merge, run the tests, commit.</p>"},{"location":"updating/#changing-your-answers","title":"Changing your answers","text":"<p>Re-run with new answers (e.g. switching default logger, or adopting a flavor):</p> <pre><code>copier update --trust -d logger=trackio   # changes the default tracker\ncopier update --trust -d flavor=tabular     # adds the benchmark harness to an existing project\n</code></pre>"},{"location":"updating/#versioning-discipline","title":"Versioning discipline","text":"<ul> <li>Commit (or stash) before <code>copier update</code> \u2014 it refuses to run on a dirty tree.</li> <li>Update one project soon after improving the template; conflicts are smallest while changes are fresh.</li> <li>The answers file records the template commit, so <code>git log</code> in the template repo tells you exactly what an update will bring.</li> </ul>"},{"location":"workflows/configs/","title":"Configure with typed configs","text":"<p>Configs are pydantic models in <code>src/&lt;pkg&gt;/configs.py</code> \u2014 typed, IDE-navigable, and validated. CLI parsing (<code>utils/cli.py</code>, one ~250-line file on tyro) keeps the Hydra-style syntax, so commands read exactly as before:</p> <pre><code>uv run python src/&lt;pkg&gt;/train.py experiment=example loss=contrastive model.lr=1e-3\n</code></pre> <p>The difference is when errors happen: a typo'd key now dies at parse time with a suggestion, and your editor autocompletes config fields.</p>"},{"location":"workflows/configs/#layout","title":"Layout","text":"<pre><code>src/&lt;pkg&gt;/configs.py       # the schema: DataConfig, ModelConfig, TrainerConfig, ...\nsrc/&lt;pkg&gt;/experiments.py   # named presets (replaces configs/experiment/*.yaml)\nconfigs/local.yaml         # machine-local overrides (gitignored)\n</code></pre>"},{"location":"workflows/configs/#the-four-override-levels","title":"The four override levels","text":"<p>Composition order (later wins): preset \u2192 local.yaml \u2192 group swap \u2192 CLI key.</p> <pre><code>flowchart LR\n    A[\"experiment=example&lt;br&gt;&lt;i&gt;preset&lt;/i&gt;\"] --&gt; B[\"configs/local.yaml&lt;br&gt;&lt;i&gt;machine-local&lt;/i&gt;\"]\n    B --&gt; C[\"loss=contrastive&lt;br&gt;&lt;i&gt;group swap&lt;/i&gt;\"]\n    C --&gt; D[\"model.lr=1e-3&lt;br&gt;&lt;i&gt;key override&lt;/i&gt;\"]\n    D --&gt; E([\"validated TrainConfig\"])</code></pre> <ol> <li>Preset \u2014 <code>experiment=example</code> selects a full config from <code>experiments.py</code>.</li> <li>Machine-local \u2014 <code>configs/local.yaml</code> (gitignored) applies on every run:    <pre><code>data:\n  num_workers: 0   # laptop\n</code></pre></li> <li>Group swap \u2014 <code>loss=contrastive</code> replaces a whole typed config block. Variants are registered in <code>GROUPS</code> in <code>configs.py</code>.</li> <li>Key override \u2014 <code>model.lr=1e-3 trainer.max_epochs=50</code>, any nesting depth, free order.</li> </ol>"},{"location":"workflows/configs/#experiments-are-python-now","title":"Experiments are Python now","text":"src/&lt;pkg&gt;/experiments.py<pre><code>EXPERIMENTS = {\n    \"base\": TrainConfig(),\n    \"example\": TrainConfig(\n        data=DataConfig(batch_size=128),\n        model=ModelConfig(hidden_dim=256, lr=1e-3),\n        trainer=TrainerConfig(max_epochs=50),\n    ),\n}\n</code></pre> <p>Building variants is ordinary code \u2014 <code>model_copy(update=...)</code>, loops over widths, helper functions \u2014 no YAML inheritance rules. Diffs in PRs stay just as reviewable, and pyright checks every preset.</p>"},{"location":"workflows/configs/#derived-values-the-replacement","title":"Derived values (the <code>${...}</code> replacement)","text":"<p>Fields that used to interpolate (<code>n_features: ${data.n_features}</code>) are now <code>None</code>-defaulted and filled in one visible place:</p> configs.py<pre><code>class ModelConfig(pydantic.BaseModel):\n    n_features: int | None = None   # derived from DataConfig unless set\n\nclass TrainConfig(pydantic.BaseModel):\n    def resolved(self) -&gt; \"TrainConfig\":\n        cfg = self.model_copy(deep=True)\n        if cfg.model.n_features is None:\n            cfg.model.n_features = cfg.data.n_features\n        return cfg\n</code></pre> <p>Inside <code>configs/local.yaml</code>, literal <code>${a.b}</code> strings still work \u2014 they resolve against the composed config:</p> <pre><code>model:\n  hidden_dim: ${data.n_features}\n</code></pre>"},{"location":"workflows/configs/#adding-a-swappable-variant","title":"Adding a swappable variant","text":"<ol> <li>Define a config class with a <code>build()</code> in <code>configs.py</code>:    <pre><code>class MaskedLossConfig(pydantic.BaseModel):\n    mask_ratio: float = 0.15\n    def build(self):\n        return MaskedColumnObjective(mask_ratio=self.mask_ratio)\n</code></pre></li> <li>Add it to the union and register it: <code>GROUPS[\"loss\"][\"masked\"] = MaskedLossConfig</code>.</li> <li><code>loss=masked loss.mask_ratio=0.3</code> now works on every entry point.</li> </ol>"},{"location":"workflows/configs/#reproducibility","title":"Reproducibility","text":"<p>Every run writes <code>config.yaml</code> into its run directory \u2014 the fully-resolved config plus git SHA, dirty flag, and the exact argv (<code>utils/run_dir.py</code>). Re-running a result months later is a file read, not archaeology.</p> <p>Debugging composition</p> <p><code>python train.py --help</code> lists every flag with its current default, plus the available experiments and group variants.</p>"},{"location":"workflows/jax/","title":"JAX","text":"<p>The template can generate a JAX project instead of a PyTorch one:</p> <pre><code>copier copy --trust -d framework=jax gh:loevlie/ml-research-template my-project\n</code></pre> <p>You get flax NNX for the model, optax for the optimizer, and the same everything-else \u2014 every command in the cheatsheet works unchanged, because only the training core swaps:</p> <pre><code>flowchart TB\n    subgraph shared [\"shared, framework-free\"]\n        S[\"configs \u00b7 CLI \u00b7 presets \u00b7 local.yaml&lt;br&gt;sweep \u00b7 tune \u00b7 seeds \u00b7 stats \u00b7 run dirs \u00b7 SLURM\"]\n    end\n    shared --&gt; PT[\"&lt;b&gt;pytorch core&lt;/b&gt;&lt;br&gt;Fabric loop \u00b7 torch model&lt;br&gt;utils/schedulers.py\"]\n    shared --&gt; JX[\"&lt;b&gt;jax core&lt;/b&gt;&lt;br&gt;jitted steps \u00b7 NNX model&lt;br&gt;optax owns the schedule\"]</code></pre> <p>JAX pairs with the generic flavor; the tabular and multimodal flavors build on PyTorch libraries (TabPFN, timm, open_clip) and stay PyTorch.</p>"},{"location":"workflows/jax/#whats-different-and-why-its-nice","title":"What's different, and why it's nice","text":"<p>The model takes its randomness explicitly. No global seed state \u2014 the init key is an argument, which is why JAX runs are reproducible by construction:</p> <pre><code>model = ExampleModel(n_features=32, hidden_dim=128, n_classes=10, rngs=nnx.Rngs(seed))\n</code></pre> <p>Three trainer knobs become optimizer composition. In the torch loop, LR scheduling, gradient clipping, and gradient accumulation are loop code. In JAX they compose into the optax chain (<code>train.py</code>), and the loop just applies updates \u2014 same config fields, less loop to maintain:</p> <pre><code>tx = optax.adamw(schedule, weight_decay=cfg.model.weight_decay)\nif cfg.trainer.gradient_clip_val:                      # trainer.gradient_clip_val=1.0\n    tx = optax.chain(optax.clip_by_global_norm(cfg.trainer.gradient_clip_val), tx)\nif accumulate &gt; 1:                                     # trainer.accumulate_grad_batches=4\n    tx = optax.MultiSteps(tx, every_k_schedule=accumulate)\noptimizer = nnx.Optimizer(model, tx, wrt=nnx.Param)\n</code></pre> <p>The training step is one jitted function. XLA compiles it on the first batch; for small models trained many steps (the tabular regime), this is where JAX outruns eager PyTorch:</p> <pre><code>@nnx.jit\ndef train_step(model, optimizer, batch):\n    def loss_fn(model):\n        out = objective(model, batch)          # same Objective protocol as torch\n        return out[\"loss\"], out\n\n    (loss, _), grads = nnx.value_and_grad(loss_fn, has_aux=True)(model)\n    optimizer.update(model, grads)\n    return loss, optax.global_norm(grads)\n</code></pre> <p>Checkpoints are msgpack files of plain arrays (<code>training_loop.py</code>) \u2014 <code>best.ckpt</code>/<code>last.ckpt</code> every epoch, <code>trainer.resume=auto</code>, and SLURM preemption recovery all behave exactly like the torch loop:</p> <pre><code>Epoch   0 | train_loss=2.3743 | val_loss=2.3773 | val_acc=0.0950\nEpoch   1 | train_loss=2.3152 | val_loss=2.3691 | val_acc=0.0950\n# rerun with trainer.resume=auto \u2192\nResumed from outputs/j1/last.ckpt (epoch 1)\nEpoch   2 | train_loss=2.2817 | val_loss=2.3671 | val_acc=0.1000\n</code></pre> <p>The loss keeps falling smoothly across the restart because the optimizer state (Adam moments, accumulation buffers, schedule position) is in the checkpoint too.</p>"},{"location":"workflows/jax/#choosing-between-the-frameworks","title":"Choosing between the frameworks","text":"<p>Pick JAX when the work is small-model/many-step (XLA fusion pays), when you have TPU access, or when the research itself is transform-shaped \u2014 <code>vmap</code> over seeds or ensemble members, per-example gradients, meta-learning. Pick PyTorch when you need the ecosystem: pretrained baselines, domain libraries, and artifacts other people can run. The longer version is in The stack.</p> <p>Single-device by design</p> <p>The JAX loop runs on one device (CPU/GPU/TPU) \u2014 no <code>pmap</code>/<code>shard_map</code> scaffolding. When a project genuinely needs sharding, reach for <code>jax.shard_map</code> and switch checkpoints to orbax; <code>save_checkpoint</code>/ <code>load_checkpoint</code> in <code>training_loop.py</code> are the only two functions that change.</p>"},{"location":"workflows/multi-seed-stats/","title":"Multi-seed statistics","text":"<p>Single-seed deltas are noise until proven otherwise. The template makes the honest version \u2014 N seeds, paired tests, effect sizes \u2014 a two-command workflow:</p> <pre><code>flowchart LR\n    A[\"run_seeds.sh&lt;br&gt;&lt;i&gt;ours, seeds 42\u20261337&lt;/i&gt;\"] --&gt; M1[\"seed_42/metrics.json&lt;br&gt;seed_123/ \u00b7 seed_456/ \u00b7 \u2026\"]\n    B[\"run_seeds.sh&lt;br&gt;&lt;i&gt;baseline, same seeds&lt;/i&gt;\"] --&gt; M2[\"seed_42/ \u00b7 seed_123/ \u00b7 \u2026\"]\n    M1 --&gt; AG[\"aggregate_seeds.py\"]\n    M2 --&gt;|--baseline| AG\n    AG --&gt; R[\"mean \u00b1 std \u00b7 95% bootstrap CI&lt;br&gt;paired Wilcoxon \u00b7 Cohen's d\"]</code></pre>"},{"location":"workflows/multi-seed-stats/#1-run-the-seeds","title":"1. Run the seeds","text":"<pre><code># locally, sequential\nbash scripts/run_seeds.sh experiment=ours seeds=\"42,123,456,789,1337,7,31,99\"\n\n# on SLURM, parallel (one array task per seed)\nsbatch scripts/sbatch_seeds.sh experiment=ours\n</code></pre> <p>Each seed writes <code>outputs/multi_seed_&lt;stamp&gt;/seed_&lt;s&gt;/metrics.json</code> (train.py saves it at the end of every run).</p> <p>For a paired comparison, run the baseline with the same seeds:</p> <pre><code>bash scripts/run_seeds.sh experiment=baseline seeds=\"42,123,456,789,1337,7,31,99\"\n</code></pre>"},{"location":"workflows/multi-seed-stats/#2-aggregate","title":"2. Aggregate","text":"<pre><code>uv run python scripts/aggregate_seeds.py outputs/multi_seed_ours \\\n    --baseline outputs/multi_seed_baseline --metric val/acc\n</code></pre> <pre><code>Metric: val/acc\nMean:   0.9234 \u00b1 0.0045\n95% CI: [0.9145, 0.9310]\n\n--- Paired Comparison ---\nOurs:     0.9234 \u00b1 0.0045\nBaseline: 0.8912 \u00b1 0.0051\nDelta:    +0.0322 **\nTest:     Wilcoxon signed-rank\nStat:     0.0000, p=0.0079\nEffect:   Cohen's d = 6.712\nSignificant at p&lt;0.01\n</code></pre>"},{"location":"workflows/multi-seed-stats/#whats-underneath-srcpkgutilsstatspy","title":"What's underneath (<code>src/&lt;pkg&gt;/utils/stats.py</code>)","text":"<ul> <li>Bootstrap CI (10k resamples) for the mean \u2014 no normality assumption.</li> <li>Wilcoxon signed-rank by default (non-parametric, paired); falls back to a paired t-test below 6 seeds, where Wilcoxon can't reach significance anyway.</li> <li>Cohen's d so reviewers see magnitude, not just a p-value.</li> </ul> <p>The functions take plain lists \u2014 reuse them for any paired comparison (per-dataset benchmark scores, ablations), as the tabular benchmark aggregator does. What each of these numbers actually tells you \u2014 and when it misleads \u2014 is on Statistics, explained.</p>"},{"location":"workflows/multi-seed-stats/#reporting-checklist","title":"Reporting checklist","text":"<ul> <li>Report <code>mean \u00b1 std (N seeds)</code> and the test used: \"0.923 \u00b1 0.005 vs 0.891 \u00b1 0.005, p &lt; 0.01, Wilcoxon signed-rank over 8 shared seeds.\"</li> <li>Same seeds for both methods \u2014 that's what \"paired\" means.</li> <li>5 seeds is the floor for a claim (the aggregator uses a paired t-test there); 8+ lets Wilcoxon speak, and more is better when the delta is small relative to the std.</li> <li>Decide the metric you'll test on before running.</li> </ul>"},{"location":"workflows/multimodal/","title":"Multimodal","text":"<p>Requires the <code>multimodal</code> flavor (adds <code>timm</code>, <code>open_clip_torch</code>, <code>transformers</code>, <code>datasets</code>).</p>"},{"location":"workflows/multimodal/#image-text-data","title":"Image-text data","text":"<p><code>src/&lt;pkg&gt;/data/image_text.py</code> builds <code>(images, token_ids)</code> loaders from any HF Hub dataset, with preprocessing matched to an open_clip backbone:</p> <pre><code>from &lt;pkg&gt;.data.image_text import create_image_text_dataloader\n\nloader = create_image_text_dataloader(\n    dataset_name=\"nlphuji/flickr30k\",\n    batch_size=64,\n    streaming=True,          # stream from the hub instead of downloading\n)\n</code></pre>"},{"location":"workflows/multimodal/#the-escalation-path","title":"The escalation path","text":"<p>Start simple; move down only when it hurts:</p> <ol> <li>HF <code>datasets</code> (this file) \u2014 fine to a few hundred GB, especially with streaming.</li> <li>LitData / MosaicML StreamingDataset \u2014 cloud-resident shards, deterministic mid-epoch resume; for serious pretraining throughput.</li> <li>WebDataset tar shards \u2014 the most interoperable archival format.</li> </ol>"},{"location":"workflows/multimodal/#contrastive-training","title":"Contrastive training","text":"<p><code>ContrastiveObjective</code> (in every project's <code>objectives.py</code>) is symmetric InfoNCE \u2014 CLIP-style:</p> <pre><code>flowchart LR\n    I[\"images x_a\"] --&gt; TI[\"image tower\"] --&gt; ZA[\"z_a&amp;nbsp;[B,D]\"]\n    T[\"texts x_b\"] --&gt; TT[\"text tower\"] --&gt; ZB[\"z_b&amp;nbsp;[B,D]\"]\n    ZA --&gt; N[\"InfoNCE: row i of x_a&lt;br&gt;matches row i of x_b\"]\n    ZB --&gt; N</code></pre> <p>It expects pairs and a two-tower model:</p> <pre><code>class TwoTower(nn.Module):\n    def forward(self, x_a, x_b):\n        return self.image_encoder(x_a), self.text_encoder(x_b)   # two [B, D] embeddings\n</code></pre> <pre><code>uv run python src/&lt;pkg&gt;/train.py loss=contrastive\n</code></pre> <p>The training loop doesn't change \u2014 that's the point of the Objective protocol. The same objective covers SimCLR-style single-modality SSL (two augmented views) \u2014 including tabular SSL with column-corruption views.</p>"},{"location":"workflows/multimodal/#backbones","title":"Backbones","text":"<ul> <li>timm for vision encoders: <code>timm.create_model(\"vit_base_patch16_224\", pretrained=True, num_classes=0)</code></li> <li>open_clip for pretrained CLIP towers and tokenizers: <code>open_clip.create_model_and_transforms(\"ViT-B-32\", pretrained=\"openai\")</code></li> <li>transformers for text encoders.</li> </ul> <p>Compose them inside your <code>models/module.py</code> two-tower module; keep the <code>@jaxtyped</code> shape annotations on <code>forward</code> so embedding-dim mismatches fail fast.</p> <p>Mixed precision</p> <p>Contrastive pretraining is bandwidth-hungry \u2014 set <code>trainer.precision=bf16-mixed</code> on Ampere+ GPUs, and use <code>trainer.accumulate_grad_batches</code> to reach contrastive-friendly effective batch sizes on small cards.</p>"},{"location":"workflows/slurm/","title":"SLURM, checkpoints &amp; resume","text":"<p>Two complementary paths ship with every project: Python launchers (submitit) for sweeps and HP search, and plain sbatch scripts for everything else. Both survive preemption without babysitting.</p>"},{"location":"workflows/slurm/#path-1-submitit-launchers","title":"Path 1 \u2014 submitit launchers","text":"<p><code>scripts/sweep.py</code> and <code>scripts/tune.py</code> take <code>--cluster slurm</code> and become job arrays \u2014 commands and details in Sweeps &amp; HP search.</p>"},{"location":"workflows/slurm/#path-2-sbatch-scripts-no-extra-deps-fully-inspectable","title":"Path 2 \u2014 sbatch scripts (no extra deps, fully inspectable)","text":"<pre><code>sbatch scripts/sbatch_train.sh experiment=example     # single job, single/multi-GPU\nsbatch scripts/sbatch_seeds.sh experiment=example     # array job: one seed per task\n</code></pre> <p>Both assume <code>.venv/</code> exists on a shared filesystem (<code>uv sync --extra dev --extra cluster</code> on the login node). Extra args are forwarded to <code>train.py</code> in the same <code>key=value</code> syntax.</p>"},{"location":"workflows/slurm/#what-gets-saved","title":"What gets saved","text":"<p>Every epoch, the loop writes two checkpoints to the run directory:</p> File When Contents <code>best.ckpt</code> val loss improved model, optimizer, scheduler, epoch, best metrics, patience <code>last.ckpt</code> every epoch same \u2014 the resume point"},{"location":"workflows/slurm/#resuming","title":"Resuming","text":"<pre><code># pick up last.ckpt from this run dir if present (no-op otherwise)\nuv run python src/&lt;pkg&gt;/train.py run_dir=outputs/myrun trainer.resume=auto\n\n# resume from an explicit checkpoint\nuv run python src/&lt;pkg&gt;/train.py trainer.resume=outputs/&lt;run&gt;/last.ckpt\n</code></pre> <pre><code>Resumed from outputs/myrun/last.ckpt (epoch 1)\nEpoch   2 | train_loss=2.2795 | val_loss=2.2928 | val_acc=0.0900\n</code></pre> <p>Resume restores the epoch counter, best-metric tracking, early-stopping patience, optimizer moments, and scheduler position \u2014 training continues as if never interrupted (modulo dataloader order within the epoch).</p> <p>Keep the trainer config consistent</p> <p>A checkpoint saved with <code>scheduler.name=cosine</code> must be resumed with the same scheduler (and vice versa) \u2014 state-dict keys are matched strictly.</p>"},{"location":"workflows/slurm/#preemption-survival-built-in","title":"Preemption survival \u2014 built in","text":"<p>The sbatch scripts pin each run's directory to the job ID and pass <code>trainer.resume=auto</code>, so a preempted job heals itself:</p> <pre><code>flowchart LR\n    A[\"job 12345&lt;br&gt;epoch 17\"] --&gt;|preempted| B[\"requeued&lt;br&gt;same job ID\"]\n    B --&gt; C[\"outputs/slurm_12345/&lt;br&gt;same run dir\"]\n    C --&gt;|\"resume=auto finds last.ckpt\"| D[\"continues at&lt;br&gt;epoch 18\"]</code></pre> <p>Granularity is one epoch; if your epochs are hours long, also wire Lightning's <code>SLURMEnvironment(auto_requeue=True)</code> for the SIGUSR1-based mid-epoch save (the scripts already send <code>--signal=B:USR1@90</code>).</p>"},{"location":"workflows/slurm/#tabular-flavor-array-benchmarks","title":"Tabular flavor: array benchmarks","text":"<p>Tabular-FM evaluation is hundreds of small CPU jobs, not one big GPU run. <code>scripts/sbatch_benchmark.sh</code> maps an estimator \u00d7 task \u00d7 fold grid onto one SLURM array, and exca's cache means a re-submitted grid recomputes only missing cells \u2014 see Tabular benchmarking.</p>"},{"location":"workflows/slurm/#cluster-notes","title":"Cluster notes","text":"<ul> <li>Logger on air-gapped compute nodes: <code>WANDB_MODE=offline</code> + <code>wandb sync</code> from the login node, or <code>logger.kind=trackio</code> / <code>logger.kind=csv</code> \u2014 see Tracking.</li> <li>Multi-node DDP: out of scope for these scripts; use the <code>scontrol show hostnames</code> + <code>torchrun --rdzv-backend=c10d</code> pattern from the PyTorch examples.</li> <li>Containers: with Pyxis/Enroot, add the container flags to the sbatch scripts directly \u2014 they're plain bash.</li> <li>Evaluating a checkpoint: <code>eval.py ckpt_path=...</code> loads just the model weights from any checkpoint, regardless of the extra training state inside.</li> </ul>"},{"location":"workflows/stats-explained/","title":"Statistics, explained","text":"<p>What the numbers in the aggregator output actually tell you, one at a time \u2014 using this run as the example:</p> <pre><code>Mean:   0.9234 \u00b1 0.0045          95% CI: [0.9145, 0.9310]\nDelta:  +0.0322  |  Wilcoxon signed-rank, p=0.0079  |  Cohen's d = 6.712\n</code></pre>"},{"location":"workflows/stats-explained/#why-seeds-at-all","title":"Why seeds at all","text":"<p>Re-running identical code with a different seed changes the init, the data order, and (on GPU) kernel nondeterminism \u2014 and the metric moves. That movement here is <code>\u00b1 0.0045</code>: any single-seed improvement smaller than a couple of those is indistinguishable from luck. Picard ran ImageNet training across 10\u2074 seeds and found the spread alone spans what papers routinely claim as a contribution (torch.manual_seed(3407) is all you need).</p>"},{"location":"workflows/stats-explained/#bootstrap-confidence-interval","title":"Bootstrap confidence interval","text":"<p>What it gives you: a range for where the true mean accuracy plausibly lies, given only your N seed results \u2014 no assumption that they're normally distributed (with 5\u201310 seeds, normality is exactly what you can't check).</p> <pre><code>flowchart LR\n    S[\"8 seed accuracies\"] --&gt;|\"resample 8 with replacement&lt;br&gt;\u00d7 10,000\"| M[\"10,000 means\"]\n    M --&gt;|\"take 2.5th / 97.5th&lt;br&gt;percentiles\"| CI[\"[0.9145, 0.9310]\"]</code></pre> <p>Reading it: if the whole experiment were repeated many times, ~95% of CIs built this way would contain the true mean. Caveat: it's a CI for the mean, not a range for individual runs \u2014 and with very few seeds it gets optimistically narrow, which is one reason 5 is the floor, not the goal.</p>"},{"location":"workflows/stats-explained/#paired-tests-and-why-the-same-seeds","title":"Paired tests \u2014 and why the same seeds","text":"<p>The question a significance test answers: could a delta this size happen by seed luck alone? Pairing makes the test sharp: because both methods ran on the same seeds, each seed contributes one difference (<code>ours \u2212 baseline</code>), and seed-to-seed variance \u2014 the biggest noise source \u2014 cancels out entirely.</p> <p>Wilcoxon signed-rank ranks those differences and checks whether they consistently favor one side. It uses no normality assumption and one outlier seed can't dominate it. The price of that robustness: with N pairs the smallest possible two-sided p is <code>2/2^N</code> \u2014 at 5 seeds that's 0.0625, so significance at 0.05 is mathematically unreachable. That's why the aggregator falls back to a paired t-test below 6 seeds (stronger assumptions, but it can at least speak). Our <code>p=0.0079</code> is the 8-seed minimum: the delta favored \"ours\" on all 8.</p>"},{"location":"workflows/stats-explained/#cohens-d","title":"Cohen's d","text":"<p>p answers \"is it real?\"; d answers \"is it big?\" \u2014 the delta measured in units of run-to-run standard deviation. <code>d = 6.7</code> means the gap is ~7\u00d7 the noise: the two distributions barely overlap (by convention 0.8 is already \"large\"). ML deltas often post huge d because seed-noise is small; the embarrassing case d protects you from is the opposite \u2014 a tiny-but- significant p hiding a difference too small to matter. With few seeds, d itself is noisy: report it, don't worship its second decimal.</p>"},{"location":"workflows/stats-explained/#further-reading","title":"Further reading","text":"<ul> <li>Deep RL at the Edge of the Statistical Precipice (NeurIPS 2021) \u2014 the case for CIs over point estimates in ML, plus rliable</li> <li>The Hitchhiker's Guide to Testing Statistical Significance in NLP (ACL 2018) \u2014 choosing the right test, incl. Wilcoxon vs t-test</li> <li>Accounting for Variance in Machine Learning Benchmarks (MLSys 2021) \u2014 what seed variance does to benchmark conclusions</li> <li>Statistics Done Wrong \u2014 short, free book on the classic inference mistakes</li> <li>Bootstrapping (Wikipedia) \u2014 the method behind the CI</li> </ul>"},{"location":"workflows/sweeps/","title":"Sweeps &amp; HP search","text":"<pre><code>flowchart LR\n    A[\"seed=42,123,456&lt;br&gt;model.lr=1e-4,1e-3\"] --&gt;|\"cross product\"| R[\"6 runs&lt;br&gt;&lt;i&gt;run_000 \u2026 run_005&lt;/i&gt;\"]\n    R --&gt;|\"default\"| L[\"local, sequential\"]\n    R --&gt;|\"--cluster slurm\"| J[\"one SLURM job array&lt;br&gt;&lt;i&gt;requeue + resume built in&lt;/i&gt;\"]</code></pre>"},{"location":"workflows/sweeps/#grid-sweeps-scriptssweeppy","title":"Grid sweeps: <code>scripts/sweep.py</code>","text":"<p>Comma-separated values fan out as a cross product (the <code>-m</code> multirun replacement):</p> <pre><code>uv run python scripts/sweep.py seed=42,123,456 model.lr=1e-4,1e-3        # 6 runs, local\nuv run python scripts/sweep.py experiment=example loss=contrastive seed=1,2,3\n</code></pre> <p>Add <code>--cluster slurm</code> and the same sweep becomes one SLURM job array via submitit (<code>uv sync --extra cluster</code>):</p> <pre><code>uv run python scripts/sweep.py --cluster slurm --partition gpu --gpus-per-node 1 \\\n    seed=42,123,456 model.lr=1e-4,1e-3\n</code></pre> <p>Every point runs with a pinned <code>run_dir</code> and <code>trainer.resume=auto</code>, so preempted jobs requeue into the same directory and continue from <code>last.ckpt</code>.</p> <p>Seed sweeps</p> <p>For seeds specifically, prefer <code>scripts/run_seeds.sh</code> / <code>scripts/sbatch_seeds.sh</code> \u2014 they write the <code>seed_&lt;s&gt;</code> layout the significance-test aggregator expects.</p>"},{"location":"workflows/sweeps/#bayesian-search-scriptstunepy","title":"Bayesian search: <code>scripts/tune.py</code>","text":"<p>Optuna drives the search; the space lives in <code>suggest()</code> inside the script:</p> scripts/tune.py (edit me)<pre><code>def suggest(trial):\n    return {\n        \"model\": {\n            \"lr\": trial.suggest_float(\"model.lr\", 1e-5, 1e-2, log=True),\n            \"hidden_dim\": trial.suggest_categorical(\"model.hidden_dim\", [64, 128, 256, 512]),\n        },\n    }\n</code></pre> <pre><code>uv run python scripts/tune.py --n-trials 20                              # local, sequential\nuv run python scripts/tune.py --n-trials 64 --workers 8 \\\n    --cluster slurm --partition gpu                                      # 8 parallel SLURM workers\n</code></pre> <p>Workers coordinate through Optuna's JournalFileBackend on the shared filesystem \u2014 no database server, and no SQLite-over-NFS corruption (Optuna's docs explicitly rule that out). Trials run in-process (<code>tune.py</code> imports <code>train.run</code>), so each trial is a normal run directory under <code>outputs/tune_&lt;study&gt;/</code>.</p> <pre><code>tune: best value 0.0850 with {'model.lr': 0.00126, 'model.hidden_dim': 128, ...}\n</code></pre>"},{"location":"workflows/sweeps/#after-the-search","title":"After the search","text":"<p>Promote the winner into a committed preset (<code>experiments.py</code>) rather than re-reading it from sweep logs \u2014 that's the version of record, and the multi-seed run for the paper starts from there.</p>"},{"location":"workflows/tabular-benchmarking/","title":"Tabular benchmarking","text":"<p>Requires the <code>tabular</code> flavor.</p> <p>Tabular foundation-model research has an unusual shape: evaluation is hundreds of small fit/predict jobs (estimator \u00d7 dataset \u00d7 fold), not one big training run. The flavor ships that harness end-to-end:</p> <pre><code>flowchart LR\n    G[\"estimator \u00d7 task \u00d7 fold&lt;br&gt;&lt;i&gt;the grid&lt;/i&gt;\"] --&gt; C{\"cached?&lt;br&gt;&lt;i&gt;config + code version&lt;/i&gt;\"}\n    C --&gt;|hit| R[\"metrics.json\"]\n    C --&gt;|miss| F[\"fit / predict&lt;br&gt;&lt;i&gt;one SLURM array task&lt;/i&gt;\"] --&gt; R\n    R --&gt; A[\"aggregate_benchmark.py&lt;br&gt;&lt;i&gt;mean ranks \u00b7 win rates \u00b7 Wilcoxon&lt;/i&gt;\"]\n    V[\"code edit?\"] -.-&gt;|\"codever bumps 0.0.N&lt;br&gt;+ writes changelog\"| C</code></pre> <p>A partial array failure costs one rerun, not the grid; a semantic code edit invalidates exactly the cells it should.</p>"},{"location":"workflows/tabular-benchmarking/#the-pieces","title":"The pieces","text":"<pre><code>src/&lt;pkg&gt;/data/openml_data.py   # OpenML task \u2192 TaskSplit (standardized CV folds)\nsrc/&lt;pkg&gt;/sklearn_wrapper.py    # your torch model behind fit/predict_proba\nsrc/&lt;pkg&gt;/benchmark.py          # typed estimator configs + one cell \u2192 metrics.json\nscripts/run_benchmark.sh        # local grid loop\nscripts/sbatch_benchmark.sh     # the same grid as one SLURM array\nscripts/aggregate_benchmark.py  # tables, mean ranks, win rates, paired Wilcoxon\n</code></pre>"},{"location":"workflows/tabular-benchmarking/#why-openml-tasks","title":"Why OpenML tasks","text":"<p>A task ID pins the dataset and its 10-fold CV splits \u2014 <code>task_id=31, fold=0</code> means the same evaluation on every machine, forever. Task IDs are the data versioning. Suites give you curated dataset collections:</p> <pre><code>from &lt;pkg&gt;.data.openml_data import tasks_in_suite\ntasks_in_suite(\"OpenML-CC18\")     # 72 classification tasks\n</code></pre> <p>(TabArena \u2014 the field's living benchmark \u2014 builds on the same task infrastructure.)</p>"},{"location":"workflows/tabular-benchmarking/#run-one-cell","title":"Run one cell","text":"<pre><code>uv run python src/&lt;pkg&gt;/benchmark.py estimator=logreg task_id=31 fold=0\n# credit-g (task 31, fold 0): acc=0.6800 balanced_acc=0.5714 fit=0.0s\n</code></pre> <p>Each cell writes <code>metrics.json</code> (acc, balanced acc, ROC AUC, log loss, fit/predict time) into its run dir.</p>"},{"location":"workflows/tabular-benchmarking/#run-the-grid","title":"Run the grid","text":"<p>Edit the grid at the top of the script (estimators, task IDs, folds), then:</p> <pre><code>bash scripts/run_benchmark.sh                 # locally, sequential\nsbatch scripts/sbatch_benchmark.sh            # SLURM array, one cell per task (CPU-sized by default)\n</code></pre>"},{"location":"workflows/tabular-benchmarking/#aggregate","title":"Aggregate","text":"<pre><code>uv run python scripts/aggregate_benchmark.py outputs/benchmark_&lt;id&gt; --baseline logreg\n</code></pre> <pre><code>=== acc (mean over folds) ===\nestimator  logreg    mlp  tabpfn\ncredit-g     0.69   0.70   0.77\n...\n\n=== Mean rank across datasets (lower is better) ===\ntabpfn 1.2   mlp 2.1   logreg 2.7\n\n--- tabpfn vs logreg (30 datasets) ---\nWin rate: 87%\nTest: Wilcoxon signed-rank, p=0.0001 ...\n</code></pre> <p>Mean ranks, win rates, and the paired Wilcoxon across datasets are the standard cross-dataset summary in tabular-FM papers; the aggregator reuses the same <code>utils/stats.py</code> as the multi-seed pipeline \u2014 here each dataset is a pair, so the test asks whether the win pattern across 30 datasets could be luck (what the numbers mean).</p>"},{"location":"workflows/tabular-benchmarking/#ship-your-model-the-way-the-field-expects","title":"Ship your model the way the field expects","text":"<p><code>sklearn_wrapper.py</code> shows the pattern: your torch model behind <code>BaseEstimator</code> (<code>fit/predict/predict_proba</code>, params stored verbatim, fitted state in <code>*_</code> attributes). That makes it a drop-in for this harness, sklearn CV, and everyone else's eval code \u2014 it's how TabPFN and TabICL got adopted. The flavor's tests include the sklearn-clone contract and a hypothesis-based row-permutation-invariance property test (the kind of invariant in-context tabular models must satisfy).</p>"},{"location":"workflows/tabular-benchmarking/#cached-cells-built-in","title":"Cached cells (built in)","text":"<p>Each cell's result is memoized on disk by exca, keyed on (estimator config, task, fold, seed, code version). Re-running a grid recomputes only missing cells \u2014 a partial SLURM-array failure costs one rerun, not the whole grid. Disable per-run with <code>cache.enabled=false</code>.</p> <p>The code-version key comes from <code>utils/codever.py</code>: it fingerprints the AST-normalized source of your package at submit time (comments/formatting/docstring edits never invalidate; semantic edits do) and auto-bumps <code>0.0.N-&lt;hash&gt;</code>, appending a unified diff of what changed to <code>cache/benchmark/code_versions/CHANGELOG.md</code>:</p> <pre><code>## 0.0.2-bdc33bb5 \u2014 2026-06-11 \u2014 git 8c3c5d1 (dirty)\n&gt; switched to balanced sampling\n-def train_step(model, batch, lr=3e-4):\n+def train_step(model, batch, lr=1e-3, accumulate=2):\n</code></pre> <p>Months later, any cached number traces to the exact code diff that produced it. Reverting code (e.g. <code>git checkout</code> of last month's commit) reproduces the old fingerprint and resurrects the old cache \u2014 no pinning needed. Annotate bumps with <code>CODEVER_NOTE=\"why\"</code>.</p> <p>Estimators are typed configs in <code>benchmark.py</code> \u2014 <code>estimator=mlp estimator.hidden_dim=64</code> swaps and overrides them with parse-time checking; add your own by defining a config class with a <code>build()</code> and registering it in <code>ESTIMATORS</code>.</p> <p>What the fingerprint does NOT see</p> <p>Dependency upgrades, data files, and notebook code don't bump the version \u2014 documented out of scope. After a <code>torch</code>/<code>sklearn</code> upgrade, force-recompute critical cells once (<code>--infra.mode=force</code> semantics live on for exactly this).</p>"},{"location":"workflows/tabular-benchmarking/#foundation-model-baselines","title":"Foundation-model baselines","text":"<pre><code>uv sync --extra tabular-baselines\nuv run python src/&lt;pkg&gt;/benchmark.py estimator=tabpfn task_id=31\nuv run python src/&lt;pkg&gt;/benchmark.py estimator=tabicl task_id=31\n</code></pre> <p>Licenses</p> <p>TabPFN weights are licensed for non-commercial research use. TabICL is fully open (weights + pretraining code) \u2014 the better base if you plan to modify the pretraining itself.</p>"},{"location":"workflows/tracking/","title":"Experiment tracking","text":"<p>Trackers sit behind one config key \u2014 pick a default at template time, switch any run with <code>logger.kind=&lt;name&gt;</code>:</p> <pre><code>flowchart LR\n    L[\"training loop&lt;br&gt;&lt;i&gt;log(name, value, step)&lt;/i&gt;\"] --&gt; K{\"logger.kind\"}\n    K --&gt;|wandb| W[\"cloud \u00b7 dashboards \u00b7 sharing\"]\n    K --&gt;|trackio| T[\"local-first \u00b7 no account\"]\n    K --&gt;|tensorboard| B[\"local \u00b7 universal\"]\n    K --&gt;|csv| C[\"flat files \u00b7 zero deps\"]</code></pre> <pre><code>uv run python src/&lt;pkg&gt;/train.py logger.kind=wandb        # cloud, collaboration\nuv run python src/&lt;pkg&gt;/train.py logger.kind=trackio      # local-first, no account\nuv run python src/&lt;pkg&gt;/train.py logger.kind=tensorboard  # local, basic\nuv run python src/&lt;pkg&gt;/train.py logger.kind=csv          # flat files, zero deps\n</code></pre>"},{"location":"workflows/tracking/#wandb-default","title":"wandb (default)","text":"<p>The free academic tier still gives Pro-level features, unlimited tracked hours, and team seats \u2014 the best UX for paper-trail experiments and sharing dashboards with an advisor. (W&amp;B was acquired by CoreWeave in 2025; terms unchanged so far. That platform risk is why the template keeps trackers swappable.)</p> <pre><code>uv sync --extra tracking-wandb\nuv run wandb login\n</code></pre>"},{"location":"workflows/tracking/#offline-slurm-nodes","title":"Offline SLURM nodes","text":"<p>Compute nodes often have no internet. Run offline, sync from the login node:</p> <pre><code>WANDB_MODE=offline sbatch scripts/sbatch_train.sh ...\nwandb sync outputs/slurm_*/wandb/offline-run-*        # later, from the login node\n</code></pre> <p>For live syncing during long runs, see wandb-osh (a small hook that relays through the head node).</p>"},{"location":"workflows/tracking/#trackio-local-fallback","title":"trackio (local fallback)","text":"<p>HuggingFace's local-first tracker with a wandb-compatible API: runs live in a local SQLite db, the dashboard is one command, no account or cloud involved \u2014 good for sensitive data and air-gapped clusters.</p> <pre><code>uv sync --extra tracking-trackio\nuv run python src/&lt;pkg&gt;/train.py logger.kind=trackio\nuv run trackio show                                   # dashboard\n</code></pre> <p>Fabric has no built-in trackio logger, so the template ships a ~50-line adapter (<code>src/&lt;pkg&gt;/utils/trackio_logger.py</code>). Optionally pass <code>space_id</code> to the adapter in <code>LoggerConfig.build()</code> (configs.py) to mirror the dashboard to a HF Space.</p>"},{"location":"workflows/tracking/#tensorboard-csv","title":"tensorboard / csv","text":"<p><code>tensorboard</code> needs no introduction (<code>tensorboard --logdir=outputs</code>). <code>csv</code> writes plain <code>metrics.csv</code> per run \u2014 zero dependencies, trivially parseable, and always there as ground truth (the multi-seed pipeline reads <code>metrics.json</code> regardless of logger).</p>"},{"location":"workflows/tracking/#what-gets-logged","title":"What gets logged","text":"<p>The loop logs <code>train/loss_step</code>, <code>train/lr</code>, <code>train/grad_norm</code> (pre-clip total norm, every 10 batches when clipping is enabled), <code>train/loss_epoch</code>, <code>val/loss</code>, <code>val/acc</code> (when the objective emits logits/targets). Add metrics by extending <code>validate()</code> in <code>training_loop.py</code> \u2014 there's no callback indirection to fight.</p> <p>Why not Aim / Neptune / MLflow?</p> <p>Neptune shut down after its OpenAI acquisition (March 2026). Aim's development stalled (Aim 4 never landed), so the template dropped it in favor of trackio. MLflow is healthy but heavier than a solo-researcher needs; the Fabric-logger seam makes it a ~40-line adapter if your lab standardizes on it.</p>"},{"location":"workflows/training/","title":"Train &amp; experiment","text":""},{"location":"workflows/training/#the-moving-parts","title":"The moving parts","text":"<pre><code>src/&lt;pkg&gt;/configs.py          # typed config schema (pydantic) + GROUPS registry\nsrc/&lt;pkg&gt;/experiments.py      # named presets\nsrc/&lt;pkg&gt;/train.py            # thin orchestrator: parse \u2192 build \u2192 loop; exposes run(cfg)\nsrc/&lt;pkg&gt;/training_loop.py    # train_epoch() + validate() \u2014 plain functions, no config inside\nsrc/&lt;pkg&gt;/objectives.py       # loss + forward bundled as a swappable callable\nsrc/&lt;pkg&gt;/data/datamodule.py  # YOUR data, behind create_dataloaders(cfg, seed)\nsrc/&lt;pkg&gt;/models/module.py    # YOUR model, behind build_model(cfg)\n</code></pre> <p><code>train.py</code> builds everything from the config and calls the loop; the loop never parses config, so it's testable with plain arguments. Your data and model live behind two factory functions \u2014 replace their bodies and the entry points never need editing (the tutorial shows this with a real dataset). Read those files once \u2014 they're short by design.</p> <p>One batch through the loop:</p> <pre><code>flowchart LR\n    L[\"train_loader\"] --&gt;|batch| O[\"objective(model, batch)\"]\n    O --&gt;|\"{'loss': ...}\"| B[\"backward&lt;br&gt;&lt;i&gt;\u00f7 accumulation&lt;/i&gt;\"]\n    B --&gt; C[\"clip grads\"] --&gt; S[\"optimizer.step()\"] --&gt; LR[\"scheduler.step()\"]\n    O -.-&gt;|\"logits + targets&lt;br&gt;(if emitted)\"| A[\"val accuracy\"]</code></pre> <p>The loop only ever sees the <code>\"loss\"</code> key \u2014 that's why swapping objectives never touches it.</p>"},{"location":"workflows/training/#run-things","title":"Run things","text":"<pre><code>uv run python src/&lt;pkg&gt;/train.py                          # base preset\nuv run python src/&lt;pkg&gt;/train.py model.lr=1e-3 seed=123   # override anything (typo-checked)\nuv run python src/&lt;pkg&gt;/train.py experiment=example       # named preset\nuv run python src/&lt;pkg&gt;/train.py trainer.precision=bf16-mixed trainer.max_epochs=200\nuv run python src/&lt;pkg&gt;/train.py --help                   # every flag + presets + variants\n</code></pre> <p>Each run writes to <code>outputs/&lt;date&gt;/&lt;time&gt;/</code> (pin with <code>run_dir=...</code>): <code>config.yaml</code> (resolved config + git state + argv), <code>best.ckpt</code>, <code>last.ckpt</code>, <code>metrics.json</code> (consumed by the multi-seed aggregator), and logger output.</p>"},{"location":"workflows/training/#experiments-as-presets","title":"Experiments as presets","text":"<p>An experiment is a typed preset in <code>experiments.py</code> \u2014 version-controlled, so every paper number traces to a commit:</p> src/&lt;pkg&gt;/experiments.py<pre><code>EXPERIMENTS = {\n    \"base\": TrainConfig(),\n    \"wider_net\": TrainConfig(\n        model=ModelConfig(hidden_dim=512, lr=1e-3),\n        trainer=TrainerConfig(max_epochs=50),\n    ),\n}\n</code></pre> <pre><code>uv run python src/&lt;pkg&gt;/train.py experiment=wider_net\n</code></pre>"},{"location":"workflows/training/#swap-the-objective","title":"Swap the objective","text":"<p>The loop calls <code>objective(model, batch)</code> and uses the returned <code>\"loss\"</code> \u2014 nothing else. To add a self-supervised / contrastive / masked objective:</p> src/&lt;pkg&gt;/objectives.py<pre><code>class MaskedColumnObjective:\n    def __init__(self, mask_ratio: float = 0.15) -&gt; None:\n        self.mask_ratio = mask_ratio\n\n    def __call__(self, model, batch):\n        x, _ = batch\n        x_masked, target, mask = mask_columns(x, self.mask_ratio)\n        loss = F.mse_loss(model(x_masked)[mask], target[mask])\n        return {\"loss\": loss}   # no logits/targets \u2192 val accuracy auto-skips\n</code></pre> src/&lt;pkg&gt;/configs.py<pre><code>class MaskedLossConfig(pydantic.BaseModel):\n    mask_ratio: float = 0.15\n    def build(self):\n        return MaskedColumnObjective(mask_ratio=self.mask_ratio)\n\nGROUPS[\"loss\"][\"masked\"] = MaskedLossConfig   # and add to the LossConfig union\n</code></pre> <pre><code>uv run python src/&lt;pkg&gt;/train.py loss=masked loss.mask_ratio=0.3\n</code></pre> <p>A CLIP-style <code>ContrastiveObjective</code> ships in every project as a worked example (<code>loss=contrastive</code> \u2014 needs a pair-yielding DataLoader and a two-tower model).</p>"},{"location":"workflows/training/#trainer-knobs","title":"Trainer knobs","text":"configs.py \u2014 TrainerConfig<pre><code>precision: \"32-true\"            # bf16-mixed for modern GPUs\nmax_epochs: 100\npatience: 10                    # early stopping\naccumulate_grad_batches: 1      # emulate larger batches\ngradient_clip_val: 1.0          # max grad norm per step; None disables\nresume: None                    # \"auto\" | /path/to/last.ckpt \u2014 see Checkpoints &amp; resume\nscheduler: name=\"none\"          # cosine | constant \u2014 warmup + per-step stepping\n</code></pre>"},{"location":"workflows/training/#evaluate-a-checkpoint","title":"Evaluate a checkpoint","text":"<pre><code>uv run python src/&lt;pkg&gt;/eval.py ckpt_path=outputs/&lt;run&gt;/best.ckpt\n</code></pre>"},{"location":"workflows/training/#built-in-sanity-tests","title":"Built-in sanity tests","text":"<p><code>uv run pytest</code> runs the research smoke tests: the model overfits a single batch, the init loss matches <code>-log(1/n_classes)</code>, shapes are right, loaders split reproducibly. Keep these passing \u2014 they catch the boring bugs that eat GPU-days.</p>"}]}